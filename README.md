A-Level NEA Project

This CLI Java Application optimally assigns taxicabs to customers, minimising the distance travelled by taxicabs to reach their assigned customers. 
This optimal matching is performed by using both the Dijkstra and Munkres-Hungarian Algorithms.
An SQLite database is used to store the data; the application thus also facilitates CRUD operations.
